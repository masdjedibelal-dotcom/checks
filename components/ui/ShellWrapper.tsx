/**
 * Re-Export aus `Shell.tsx`: MaklerContext + URL-Parameter (`name`, `firma`, `farbe`, …).
 * Zusätzliche Props: `isDemoMode`, `slug` — für `/demo/[slug]` (Kontakt → Kauf-CTA).
 */
export { default } from "./Shell";
